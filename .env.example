# Copy this file to .env and fill in your actual values
# The .env file should be added to .gitignore to keep your secrets private

# Cloudflare Zone ID (you can find this in your Cloudflare dashboard)
CF_ZONE_ID=your-cloudflare-zone-id-here

# Your domain name (e.g., example.com, mydomain.org)
CF_DOMAIN=your-domain-name-here

# Optional: API token for request authentication (if you want to require auth)
# API_TOKEN=your-optional-api-token-here
