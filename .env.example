# Cloudflare Zone ID for DNS management
# Get this from your Cloudflare dashboard -> Overview -> Zone ID
CF_ZONE_ID=your-cloudflare-zone-id-here

# Domain name for DNS records
# This will be appended to hostnames (e.g., hostname.yourdomain.com)
CF_DOMAIN=yourdomain.com

# KV Namespace binding name (must match the "binding" in wrangler.toml)
# This should match the binding name in your [[kv_namespaces]] section
KV_NAMESPACE=IP_STORE

# Note: Secrets like CF_API_TOKEN and API_TOKEN should be set using:
# npx wrangler secret put CF_API_TOKEN
# npx wrangler secret put API_TOKEN
